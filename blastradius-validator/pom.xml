<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>io.github.baokhang83.blastradius</groupId>
        <artifactId>blastradius-parent</artifactId>
        <version>0.3.2</version>
    </parent>

    <artifactId>blastradius-validator</artifactId>
    <packaging>jar</packaging>

    <name>blastradius-validator</name>
    <description>Shadow-mode dependency-based test selection validator</description>

    <dependencies>
        <dependency>
            <groupId>io.github.baokhang83.blastradius</groupId>
            <artifactId>blastradius-core</artifactId>
            <version>${project.version}</version>
        </dependency>
        <dependency>
            <groupId>org.eclipse.jgit</groupId>
            <artifactId>org.eclipse.jgit</artifactId>
            <version>${jgit.version}</version>
        </dependency>
        <dependency>
            <groupId>com.fasterxml.jackson.core</groupId>
            <artifactId>jackson-databind</artifactId>
            <version>${jackson.version}</version>
        </dependency>

        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter</artifactId>
            <scope>test</scope>
        </dependency>
        <!-- FixtureProjectBuilder, moved into blastradius-core's test-jar (T006) since
             it's now shared test-support code, not validator-only. -->
        <dependency>
            <groupId>io.github.baokhang83.blastradius</groupId>
            <artifactId>blastradius-core</artifactId>
            <version>${project.version}</version>
            <type>test-jar</type>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>${compiler.plugin.version}</version>
                <configuration>
                    <!-- Retains record/constructor parameter names in bytecode, so
                         Jackson can deserialize records via their canonical
                         constructor without @JsonCreator/@JsonProperty annotations. -->
                    <parameters>true</parameters>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>${surefire.version}</version>
            </plugin>
            <plugin>
                <!-- shade:shade repackages the EXISTING main-artifact jar; it does not
                     produce one itself. jar-plugin's early execution below builds that
                     base jar first, in the same phase, before shade runs. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>${jar.plugin.version}</version>
                <executions>
                    <execution>
                        <id>build-base-jar-early</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>jar</goal>
                        </goals>
                    </execution>
                    <!-- The implicit "default-jar" execution (bound to the package phase
                         for jar packaging) would otherwise run after maven-shade-plugin's
                         "shade-at-package" execution has already replaced the project's
                         main artifact, and fail with "You have to use a classifier to
                         attach supplemental artifacts". Disable it; build-base-jar-early
                         above already covers the plain-jar build. -->
                    <execution>
                        <id>default-jar</id>
                        <phase>none</phase>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <!-- A -javaagent jar only puts ITSELF on the classpath, not its Maven
                     dependencies (JGit, Jackson, junit-platform-launcher, blastradius-core)
                     — so the final artifact must be a self-contained shaded (fat) jar, not
                     a plain jar, or DependencyTrackingAgent fails NoClassDefFoundError the
                     moment it references TestBoundaryListener. -->
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>${shade.plugin.version}</version>
                <configuration>
                    <artifactSet>
                        <!-- JUnit Platform is supplied by each target Surefire/Failsafe fork.
                             Do not put our compile-time copy ahead of that runtime on the system
                             classpath, or JUnit's alignment check rejects mixed versions. -->
                        <excludes>
                            <exclude>org.junit.platform:*</exclude>
                            <exclude>org.junit.jupiter:*</exclude>
                            <exclude>org.opentest4j:*</exclude>
                            <exclude>org.apiguardian:*</exclude>
                        </excludes>
                    </artifactSet>
                    <filters>
                        <!-- Strip signature files from shaded dependencies — a merged
                             uber-jar no longer matches their original signatures,
                             which otherwise fails with a SecurityException at load. -->
                        <filter>
                            <artifact>*:*</artifact>
                            <excludes>
                                <exclude>META-INF/*.SF</exclude>
                                <exclude>META-INF/*.DSA</exclude>
                                <exclude>META-INF/*.RSA</exclude>
                            </excludes>
                        </filter>
                    </filters>
                    <!-- This jar is attached via -javaagent to every JVM in a target
                         build, including Maven's own launcher process (JAVA_TOOL_OPTIONS
                         applies to all of them, not just forked test JVMs) — so its
                         bundled dependencies must never collide with same-named classes
                         Maven or the target project resolve themselves. Left unrelocated,
                         a real run against apache/shenyu found this bundled slf4j-api
                         (pulled in transitively by JGit) wins Maven's own parent-first
                         classloader lookup for org.slf4j.LoggerFactory, since -javaagent
                         jars land on the system classloader — a parent of Maven's own
                         plexus-classworlds realm. SLF4J's provider binding happens once,
                         globally, per JVM, so that one resolution silently locks Maven's
                         own logger to a no-op backend for the rest of the build: every
                         "[INFO] Scanning for projects..." line and any real build/test
                         failure Maven would otherwise report vanish, leaving only "failed
                         to read dependency record" with no clue why. org.junit,
                         org.opentest4j and org.apiguardian are deliberately excluded from the
                         shaded artifact above: TestBoundaryListener registers as a real
                         org.junit.platform.launcher.TestExecutionListener via the
                         target's own JUnit Platform SPI, which requires exact interface
                         identity with the target's own (unrelocated) JUnit Platform
                         classes — relocating them would silently break that registration. -->
                    <relocations>
                        <relocation>
                            <pattern>org.slf4j</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.slf4j</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.eclipse.jgit</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.jgit</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.fasterxml.jackson</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.jackson</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>com.googlecode</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.googlecode</shadedPattern>
                        </relocation>
                        <relocation>
                            <pattern>org.apache</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.apache</shadedPattern>
                        </relocation>
                        <!-- Matched by fully-qualified class name, not just imports, so
                             DependencyTrackingAgent's own string-literal prefix check
                             (INSTRUMENTATION_LIBRARY_PACKAGE_PREFIX) must be kept in sync
                             with this shadedPattern by hand — shade only rewrites actual
                             class references, never string literals. -->
                        <relocation>
                            <pattern>org.objectweb.asm</pattern>
                            <shadedPattern>io.github.baokhang83.blastradius.shaded.asm</shadedPattern>
                        </relocation>
                    </relocations>
                    <transformers>
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                            <mainClass>io.github.baokhang83.blastradius.validator.cli.Main</mainClass>
                            <manifestEntries>
                                <!-- Enables this jar to also be used as -javaagent:...jar=<outputPath> -->
                                <Premain-Class>io.github.baokhang83.blastradius.core.tracking.DependencyTrackingAgent</Premain-Class>
                                <Can-Redefine-Classes>true</Can-Redefine-Classes>
                                <Can-Retransform-Classes>true</Can-Retransform-Classes>
                            </manifestEntries>
                        </transformer>
                        <!-- Merges META-INF/services files instead of one overwriting
                             another, so our TestExecutionListener registration survives
                             shading alongside any dependency's own service files. -->
                        <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer" />
                    </transformers>
                </configuration>
                <executions>
                    <!-- Integration tests need a real, shaded agent jar to attach to a
                         subprocess mvn run — but `package` runs after `test` in the
                         default lifecycle. Build it early too, identically configured,
                         so it exists in time. -->
                    <execution>
                        <id>build-agent-jar-early</id>
                        <phase>process-test-classes</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>shade-at-package</id>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
